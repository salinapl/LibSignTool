title: Events Page
icon: 🎉
fields:
  embed:
    label: Embed URL
    type: url
  start:
    label: Active on
    type: date
    required: true
    width: 1/4
  expire:
    label: Inactive on
    type: date
    required: true
    width: 1/4
  tags:
    label: Campaign Category
    type: select
    accept: options
    options: query
    query: page.parent.tags.split(',')